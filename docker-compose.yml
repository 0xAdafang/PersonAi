services:
  go-api:
    build: ./go-api
    ports:
      - "8080:8080"
    depends_on:
      - rust-core
      - python-llm

  rust-core:
    build: ./rust-core

  python-llm:
    build: ./python-llm
    ports:
      - "11434:11434"  # Port d’Ollama
